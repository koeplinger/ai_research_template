# Prompt NNN: <!-- short description -->

*Created D Month YYYY; updated D Month YYYY.*

<!-- Immutable once committed: the updated date never moves. Write the
     entry as the first act of the round; complete it before the
     researcher commits. Delete these comments in the instance. -->

--- PROMPT START ---
<!-- The prompt verbatim and unedited; this region is skipped by the
     editorial checks. An attachment is recorded by locator, not copied.
     Where MANIFESTO.md section 6 keeps material out of the repository,
     replace it with:
     [removed before logging: <what, in a form the access conditions
     allow>; <shelfmark, release, or version>; <access conditions>] -->
--- PROMPT END ---

## What was done

<!-- The round's work, by artifact: what was created, what was changed,
     what was brought to the researcher and left for authorization. Cite
     claims, checks, and plans by token; name the authorizing prompt of
     any change to a frozen artifact (MANIFESTO.md section 12). -->

## Corrections and departures

<!-- The reason for any correction made in the round, and for any
     departure from the rules; where none, write "none". This is where
     the reason lives (MANIFESTO.md section 12). -->

## Closing status

<!-- The status block of the round's final reply, copied as sent
     (MANIFESTO.md section 16): DONE, WAITING ON YOU, or IDLE lines, one
     or more, IDLE only alone; never RUNNING, since the round has closed.
     It sits outside the header block, so its labels are not header
     fields (ONTOLOGY.md section 1.1). -->

```
DONE: <!-- what finished -->
```
