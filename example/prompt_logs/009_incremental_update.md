# Prompt 009: two incremental updates the fence allows

*Created 8 September 2026; updated 8 September 2026.*

--- PROMPT START ---
The template has changed since this example was made, and its close-out
sweep read the example against it. Do not re-run the example and do not
re-do it: apply only what the fence in README.md already allows, a
re-copied tool and a corrected sentence, and record them here. Bring me
the rest.
--- PROMPT END ---

## What was done

Two updates, both of the kind *The fence* names as allowed:

- **A re-copied tool.** The template corrected the fixture its tools build
  their scratch project from: it read the project's own configuration and
  required the mutation set to be unfilled, which it is only in a template.
  This project filled its mutation set in round 002, so nine of its tools
  failed their own `--selftest` with an assertion. The corrected function
  was copied into `tools/lint_docs.py` from the template, and the nine now
  pass: the linter, the round check, the concordance check, the probe, the
  run ledger, the residue check, the session brief, the ontology queries,
  and the claim sites.
- **A corrected sentence.** `LICENSE.md`, *In a project built from this
  template*, told a project to replace the recommended citation with its
  own. The written material is under CC BY 4.0, which requires credit to
  the original, so a project following that instruction would have been out
  of compliance with the license it inherits. It now says to add the
  project's own and keep the template's, and says why.

Brought to the researcher and not applied, since each would be a change to
the example's substance rather than a re-copied tool or a corrected
sentence: that this project's rulebooks are the template's as of commit
`a460878` and no longer match the template's current text, which is what
the fence is for; and the choice between leaving the example as it stands,
re-instantiating it with the current tools and copying this record over,
and re-doing it.

## Corrections and departures

None. Both updates are what `README.md`, *The fence*, permits without a
re-run, and this entry is the record it requires.

## Closing status

```
DONE: the tool re-copied and the license sentence corrected, both recorded
WAITING ON YOU: whether this example stays as it is, is re-instantiated, or is re-done
```
