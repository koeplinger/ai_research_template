# Prompt 008: the review of the project as a whole, and its corrections

*Created 6 September 2026; updated 6 September 2026.*

--- PROMPT START ---
Before the first commit, three readers reviewed this project as a whole,
read-only: a newcomer, the consistency sweep of CHECK_METHODOLOGY.md
section 7, and a reader of the project's contract with the rulebooks and
with the rule that everything here is synthetic and generic. Hand their
substantive findings to refuters, apply what survives at my word, and
record here what changed and why. The mechanical findings (dates,
placeholders left from the templates, slot values pasted into prose,
leftover build text) apply without refutation; say so.
--- PROMPT END ---

## What was done

The three readers returned 63 findings, many the same defect seen from
two sides. Thirteen substantive ones went to refuters, one reader per
finding: seven stand, five are narrowed, one is refuted (that the note
"stands in" for the public record; the rulebooks place a collated
statement of a source at tier 2 by rule, so nothing stands in for
anything).

Applied before the first commit, so that no artifact was yet immutable
or frozen in the rulebooks' sense, and at the researcher's word:

- The source was renamed throughout, from "the register" to "the market
  roll" ([Roll1851], `source_documents/Roll1851_market_roll.csv`,
  `python_project/src/make_roll.py`), since the rulebooks reserve
  *register* for a claim's trust level and the collision met a newcomer
  in the first paragraph. Entries 001 to 007 of this log carry the new
  word; nothing else in them changed.
- Every second pass recorded as the researcher's now says who performed
  it: the assistant in the researcher's role (claims 001, 002, 004, and
  007; checks 001 to 004; the protocol note). `README.md`, *Two roles,
  who played them*, and the standing directions of `ONBOARDING.md` say
  the same.
- [check 003] gained the positive control its record named and its
  program lacked: one planted repeat, counted as one; its record's part
  3 now describes the assertion the program makes (at least 51 rows),
  and part 6 records what the bound mutation does (three repeats, the
  verdict flips to reproduced), from a run of the assertions on the
  mutated list. [claim 004] was re-assigned RULED_OUT on the strengthened
  check.
- The pre-registration stamp of [check 001] was withdrawn: the roll was
  generated to a named count in round 002, before the protocol note of
  round 003, so the note fixes the rule of reading and not the outcome;
  the note, the check's part 1, the notes index, and the released note's
  sentence say so.
- The standing reservation of plan 002 was dropped, and [claim 005]'s
  Reserved line with it: no established claim's scope depends on the
  note's convention, and the plan template keeps the section only where
  frozen claims rest on the deferred question. The ledger's cautions on
  T001 and T002 name the plan and the candidate claims instead.
- T003 entered: the roll holds no row for Tuesday 12 August 1851, against
  the note's interruptions at the turn of the year and in the depth of
  winter [claim 007]. F001 restricted to the count alone, which is what
  [claim 001] establishes; T001 carries the comparison.
- Task 5 of plan 001 no longer narrates the incident that prompted it;
  the ledger row carries "added while engaged, prompt 006". The plan's
  sanity section counts three programs. [claim 003] records a route that
  checks the division (the read-back 4.25 x 12 = 51). [claim 006] points
  at plan 002's task 1, the recount. [claim 007] and [check 004] state
  the calendar as the one the check imposes.
- The rulebooks' definition of a release now states the first-version
  case (no changelog; the stamp alone releases it), where `released` is
  defined and where a rebuild's ledgers are placed; the template was
  amended in the same words.

Applied without refutation, as directed: the reading order for a
newcomer, the key to the tokens, and the running instructions in
`README.md`; the slot values pasted into running prose replaced by short
handles, the full values staying in the Slots tables, and a voice record
of this project's own; the updated dates of every copied file the
instantiation or a round changed set to 6 September 2026; the roadmap's
line of inquiry written and its status column carrying the full status
line; the claims index's subject cells stripped of register words; the
inherited ledger stating "none"; the harness README's build-lens
sentence, the governance map's row about the worked example, and the
configuration's row for the build plan removed; the manifesto's lineage
line naming the template; the requirements file pinning what ran;
`VISION.md` kept, since `ONTOLOGY.md` and the governance map cite it.

Corrections to earlier entries, which are immutable once committed and
are completed here: entry 001 says the vision was left behind; it was
carried over later in the same round, for the reason above. Entry 001
lists eleven of the fifteen slot values; the four it omits, each
confirmed in that round, are the reply hook (the tracked settings file of
the harness), adequate references (a held source cited to its row or
page; a check for any count), the voice (now three rules of this
project's own), and the reference fields (Authors, Title, Where, Year,
Identifier, Keywords, Standing). A build-lens block is a passage of the
template's rulebooks addressed to the template's own construction,
stripped at instantiation. Entry 005 records a reservation and F001's
wording that this round undid, as above. Entry 007 says the probe caught
every bound mutation; it judged the mutations of checks 001 and 004 and
did not judge check 003's, whose direct run fails by design; that
mutation is now recorded in the check's own part 6.

## Corrections and departures

The frozen artifacts of plan 001 and the released note were corrected in
their running text at the researcher's word, as `MANIFESTO.md` section 12
allows, with the reasons above; each keeps its dates, since all of it
precedes the first commit. The mechanical findings were applied without
a refuter, a departure from `CHECK_METHODOLOGY.md` section 7 directed by
the prompt, since a wrong date or a template placeholder has no
counter-case to argue.

## Closing status

```
DONE: the review's findings refuted or applied; the record corrected at the researcher's word; every gate clean
```
